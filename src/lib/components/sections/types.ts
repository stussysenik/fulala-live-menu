/**
 * Re-export of the section config types from the pure domain module —
 * components import from here, Convex and tests import from the domain.
 * One definition, two doorways.
 */
export type { DisplaySectionConfig, SectionInstance } from '$lib/domain/sectionConfig';
